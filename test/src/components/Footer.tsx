import React from "react";

const Footer: React.FC = (): JSX.Element => {
  return (
    <p className="read-the-docs">
      Next generation health management library to replace useState.
    </p>
  );
};

export default Footer;
